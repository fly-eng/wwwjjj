// 修复找不到“node”的类型定义文件问题，可通过执行命令 `npm install --save-dev @types/node` 安装 `@types/node` 来解决，这是因为在 `compilerOptions` 中指定了类型库 "node" 的入口点。
// 可通过命令 `npm install --save-dev @types/node` 进行安装
{
  "compilerOptions": {
    "types": ["node"],
    "paths": {
      "@/*": ["./src/*"]
    }

  }
}
